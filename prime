#include<iostream>
using namespace std;
int main(){
	int num,i,a=0;
	cout<<"Enter a number:";
	cin>>num;
	for(i=2;i<num;i++){
		if(num%i==0){
			a++;
			break;}}
	if(a==0){
		cout<<"\n It is prime.";}
	else{
		cout<<"\n It is not a prime.";}
	return 0;
}
